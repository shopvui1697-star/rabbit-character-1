[project]
name = "rabbit3-backend"
version = "0.1.0"
description = "Rabbit3 — Agentic Voice Assistant Backend"
requires-python = ">=3.12"

dependencies = [
    "pydantic-ai[bedrock,openai]",
    "fastapi",
    "uvicorn[standard]",
    "httpx",
    "pydantic-settings",
    "logfire[pydantic-ai]",
    "boto3",
    "mcp[cli]",
    "asyncpg",
]

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-asyncio",
    "ruff",
    "mypy",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
strict = true
